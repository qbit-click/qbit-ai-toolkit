# Generic profile

`installer.codexpro` currently exposes a single generic deployment profile. It does not copy repository-specific application templates; the profile exists to satisfy the shared installer contract while all Windows deployment behavior is driven by installer parameters and the managed launcher/configuration assets.
