# Rust profile

Preserves the published Rust profile name. The installed semantic runtime
remains the shared PowerShell, Bash, and Python capability and never runs Cargo
in the target repository root.
